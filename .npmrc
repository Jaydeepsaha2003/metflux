# Force npm to install devDependencies even when the host sets
# NODE_ENV=production or npm_config_production=true. Hostinger's Node.js
# Git-deploy sets that by default, which would skip typescript / vite /
# tailwind in any workspace and break `npm run build`.
production=false
